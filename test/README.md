# اختبار قواعد الأمان

يشغّل قواعد `firestore.rules` على محاكي Firestore ويجرّب عليها الهجمات الحقيقية
والعمليات المشروعة معاً — لأن قاعدة ترفض تسجيل حضور صحيح أسوأ من قاعدة متساهلة.

    cd test && npm install && npm test

يحتاج Java (المحاكي يعمل على JVM). آخر تشغيل: 69 اختباراً، كلها ناجحة.
