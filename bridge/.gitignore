# ⚠️ مفتاح خدمة Firebase — صلاحية كاملة على قاعدة البيانات، يتجاوز كل
#    قواعد الأمان. رفعه إلى مستودع عام يعني تسليم بيانات الموظفين والرواتب
#    لأي أحد. لا يُرفع أبداً.
serviceAccountKey.json
*serviceAccount*.json

# مسار بايثون المحمول — يختلف بين الأجهزة
python-path.txt

# نسخة بايثون محمولة موضوعة داخل المجلد
python/

# حالة المؤشّر — خاصة بكل جهاز، ولا معنى لمشاركتها
zk_state.json
zk_state.json.tmp

# سجلّات الخدمة
logs/
*.log

# NSSM يُنزَّل محلياً
nssm.exe

__pycache__/
*.pyc
