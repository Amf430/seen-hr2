# seen-hr2 — سياق المشروع وخطة العمل

> هذا الملف يُقرأ تلقائياً في كل جلسة. **اقرأه كاملاً قبل أول تعديل.**
> تفاصيل كل مرحلة من خطة العمل في [`PROMPT-new-features.md`](./PROMPT-new-features.md) —
> افتح قسم المرحلة المطلوبة منه قبل أن تبدأ العمل عليها.

---

## ما هو النظام

نظام موارد بشرية عربي (RTL) لشركة سعودية: حضور وانصراف بالموقع الجغرافي والبصمة،
طلبات استئذان وإجازات بسلسلة اعتماد، مسير رواتب على دورة ٢٦←٢٥، ملفات موظفين
ومستندات، وتقارير.

**التقنية:** JavaScript خالص — ES modules، **بلا build step وبلا إطار وبلا npm في
الإنتاج**. Firebase v10 من CDN (Firestore + Auth). الاستضافة Firebase Hosting على
**الخطة المجانية (Spark)**.

**التوقيت:** الرياض UTC+3. الدورة المالية من ٢٦ إلى ٢٥.

---

## القواعد الذهبية — لا تُكسر

1. **`firestore.rules` هي الحماية الوحيدة الحقيقية.** كل ما في `js/lib/perms.js` يخفي
   أزراراً فقط — الكود يعمل داخل متصفح الموظف. أي ميزة جديدة = قاعدة جديدة على السيرفر،
   لا شرط في الواجهة.
2. **لا Cloud Functions. لا خطة Blaze.** أي حل يحتاج كوداً على الخادم مرفوض — اقترح
   بديلاً يعمل من المتصفح مع قاعدة تحرسه.
3. **لا ترحيل بيانات إجباري.** كل حقل جديد له سلوك افتراضي عند غيابه **مطابق لسلوك اليوم
   بالحرف**. النظام يشتغل يوم النشر بلا لمس وثيقة واحدة. النموذج المرجعي:
   `branchesOf()` في `js/lib/geo.js` — تركّب الشكل الجديد من القديم في الذاكرة.
4. **`ymdKsa()` لا `ymd()`** في أي شيء يخصّ الحضور. القاعدة `todayKsa()` في
   `firestore.rules` تعتمد توقيت الرياض، وأي تباعد يرفض كل كتابة من جهاز على منطقة
   زمنية أخرى.
5. **التعليقات العربية تشرح «لماذا» لا «ماذا».** احفظها. لا تحذف تعليق ⚠️ أبداً بلا أن
   تشرح لي لماذا لم يعد صحيحاً. أضف مثلها لكل قرار غير بديهي تتخذه.
6. **كل استعلام لازم يطابق قاعدته حرفياً.** Firestore يرفض الاستعلام **كاملاً** ما لم
   يكن مقيَّداً بحيث تُحقّق كل نتيجة محتملة شرط القاعدة. استعلام غير مقيَّد = شاشة فارغة
   مع خطأ صلاحيات في الإنتاج، لا نتيجة منقوصة.
7. **كل حقل نصّي أو مصفوفة يكتبها المستخدم عليه سقف في القاعدة.** حقل بلا سقف = تخزين
   مجاني على حساب المالك. النموذج: `selfContactOk()` في `match /users`.
8. **`localStorage` لتفضيلات هذا الجهاز فقط** — لا شيء يخصّ الحقيقة.
9. **قبل أي كود:** اقرأ الملفات ذات العلاقة، واعرض خطة مختصرة (الملفات التي ستلمسها +
   شكل البيانات + القواعد الأمنية) وانتظر الموافقة.

---

## الأوامر

```bash
# الاختبارات (من داخل test/)
cd test && npm install
npm run test:pure     # ٧ ملفات node خالصة — أجزاء من الثانية، شغّلها دائماً
npm run test:rules    # firestore.rules على المحاكي — يحتاج Java
npm test              # الكل

# ⚠️ محاكي القواعد يحتاج Java على PATH — openjdk من Homebrew «keg-only»:
export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"

npx eslint js/        # من الجذر

# النشر — ⚠️ مساران مختلفان، لا أمر واحد
#
#   الواجهة (index.html · js · css · img)
#     تُنشر عبر **GitHub Pages من فرع `main`، المسار الجذر**.
#     لا أمر نشر لها: وصولُ الكود إلى main هو النشر.
#     ⚠️ لا تستعمل `firebase deploy --only hosting` — الاستضافة ليست
#     Firebase. وترويسات CSP في firebase.json **لا تصل المستخدم** لأن
#     GitHub Pages لا يرسلها (تحقّقتُ: لا CSP على الموقع الحيّ). تبقى في
#     الملف للمستقبل ولا يُبنى عليها أمان.
#
#   القواعد والفهارس — ليست ملفات يخدمها Pages، فتُنشر بأمر منفصل:
firebase deploy --only firestore:indexes
firebase deploy --only firestore:rules
```

**ترتيب النشر إلزامي:** الفهارس ← القواعد ← ثم ترقية `dev → main`.
عكسُه يُوصل الواجهة الجديدة للموظفين قبل القواعد، فتفتح كل شاشة جديدة على
خطأ صلاحيات.

⚠️ `payroll.js` و`attendance.js` تجرّان `firebase.js` وقت الاستيراد (SDK من gstatic)
فلا تعملان في node إطلاقاً. تُختبران في المتصفح عبر `test/browser/suite.html`.

خط الأساس الحالي: **١١٠٢ اختباراً كلها ناجحة** (٤١٠ قواعد · ٥٩٥ خالص · ٩٧ متصفح).
أي انخفاض عن هذا الرقم يعني شيئاً كُسر.

**حدّث هذا السطر مع كل دفعة اختبارات** — خط أساس قديم أسوأ من غيابه، لأن انخفاضاً
حقيقياً تحته يبدو ارتفاعاً. (كان ٤٦٥ وصار قديماً بصمت مرّةً.)

---

## أين وصلنا — آخر تحديث ٢٠٢٦-٠٨-١٣

**المراحل الاثنتا عشرة كلها مُنجزة.** الحالة التفصيلية:

| | |
|---|---|
| الفرع | `feat/map-picker-and-indexes` (مقطوع من `dev`) |
| كوميتات لم تُدفع | **١٦** — لا شيء على GitHub بعد |
| `dev` مقابل `main` | متطابقان — لا عمل لأحد غيرنا معلَّق |
| قواعد الإنتاج | **قديمة بـ ٢٨٤ سطراً** — لا تعرف `tasks` ولا `announcements` ولا `calendarEvents` |
| الفهارس | **١٠ فهارس مكتوبة، صفر منشور** |

### الخطوات المتبقية بالترتيب

1. `git push -u origin feat/map-picker-and-indexes`
2. `gh pr create --base dev` ← **`--base dev` صراحةً**، وإلا نشر للموظفين فوراً
3. بعد الدمج: `firebase deploy --only firestore:indexes` ثم `--only firestore:rules`
4. ترقية `dev → main` — **بكلمة المالك وحده**، وهي لحظة وصول الميزات للموظفين

### ما يعمل بلا نشر قواعد، وما لا يعمل

يعمل: الشفتات المتعددة · قفل الحضور المتأخر · خريطة الفرع — لا تحتاج مجموعات جديدة.
يسقط بخطأ صلاحيات: المهام · الإعلانات · التقويم · أداء القسم · إضافة موظف بواسطة
المدير · طلب تصحيح البصمة.

### ما فُحص في متصفح حقيقي وما لم يُفحص

فُحص على المحاكي بالأدوار الثلاثة: المهام (الدورة كاملة) · أداء القسم · الإعلانات ·
سياسة الإجازات · قفل الحضور · الخريطة · القوالب المتكرّرة · التفويض · سجل الوقت ·
تقويم الفريق والأحداث.

**لم يُفحص:** شاشة الجوال إطلاقاً · الأرشيف على بيانات حقيقية · تنفيذ ترحيل الإجازات
(**غير موصول عمداً** — بانتظار موافقة المالك على جدول «قبل/بعد» على بيانات حقيقية).

### ⚠️ مكشوف علناً على الموقع الحيّ

`firestore.rules` و`firebase.json` و`eslint.config.mjs` تُقرأ من الإنترنت اليوم
(`https://amf430.github.io/seen-hr2/firestore.rules` → 200)، لأن Pages ينشر جذر
المستودع كاملاً. وترقية `main` ستضيف `CLAUDE.md` و`firestore.indexes.json` إليها.

القواعد ليست سرّاً بذاتها (تُفرض على السيرفر)، لكن تعليقاتها تقول صراحةً أين الحدود
في الواجهة وحدها — وهي خريطة لمن يريد تجاوزها. **الإصلاح المقترح ولم يُنفَّذ:**
`_config.yml` في الجذر يستثنيها من نشر Jekyll.

---

## قرارات المالك — لا تُناقَش مجدداً

| التاريخ | القرار | ما ترتّب عليه |
|---|---|---|
| ٢٠٢٦-٠٨-١٢ | **الحضور المتأخر مسموح** لمن لم يسجّل اليوم إطلاقاً، ومخفي لمن عنده جلسة | `allowLateCheckIn = true`، ووسم `lateCheckIn` على السجل |
| ٢٠٢٦-٠٨-١٢ | **زر واحد متحوّل** لا زرّان | `attendButtonState()` النقيّة، والانصراف لا يُقفل أبداً |
| ٢٠٢٦-٠٨-١٢ | **مدير القسم يرى الرواتب** — «ما فيه مشكلة» | أُلغي ترحيل `users/{uid}/private/comp` بالكامل. يقرأ ولا يكتب |
| ٢٠٢٦-٠٨-١٢ | **تجاهُل قاعدة «انتظر أسبوعين»** قبل توسعات المهام | المرحلة ٧ بُنيت مع ٥ |
| ٢٠٢٦-٠٨-١٢ | **الموظف لا يرى إجازات زملائه إطلاقاً** | حُذفت وثيقة `teamAway` المنشورة وتنبيه «٣ من زملائك»، واستُبدلت بـ`calendarEvents` |

---

## المعمارية

```
index.html            نقطة الدخول الوحيدة — تحمّل js/main.js
js/config/
  firebase.config.js  مفاتيح المشروع
  pages.js            سجل الصفحات والتنقّل — مصدر الحقيقة الوحيد. صفحة جديدة تُعرَّف هنا فقط
js/app/
  router.js           يربط معرّف الصفحة بدالة render(view, token)
  shell.js            القشرة والقائمة الجانبية والشارات
  subscriptions.js    اشتراكات onSnapshot اللحظية — مختلفة لكل دور
js/lib/               منطق نقي قابل للاختبار بـ node
  state.js            الحالة في الذاكرة: getMe · getUsers · getSettings · getRequests
  shifts.js  ⚠️       حلّ الوردية — يحدّد من هو متأخر ومن هو غائب
  attendance.js ⚠️    الجلسات والحالة اليومية
  payroll.js ⚠️       المسير
  requests.js ⚠️      الطلبات والأرصدة — معاملات
  perms.js            صلاحيات الواجهة (إخفاء أزرار فقط)
  geo.js              الفروع والنطاق الجغرافي
  users.js · settings.js · documents.js · certificates.js · notifications.js
  hr-stats.js · leaderboard.js · adjustments.js · excel.js · audit.js
  dates.js · format.js · dom.js · ui.js · icons.js · lifecycle.js · nav.js
  ── وحدات نقيّة أُضيفت في المراحل الجديدة (تُختبر في node) ──
  task-flow.js        دورة حياة المهام · التكرار · الاعتماديات · التفويض · الأرشيف
  team-stats.js       تجميع أداء القسم — يستهلك buildDailyStatus ولا يعيد الحساب
  leave-balance.js ⚠️  اشتقاق الرصيد والاستحقاق التدريجي — يقرّر أرصدة الناس
  zk-coverage.js      تغطية حقل department في سجل الجهاز
  calendar.js         شبكة التقويم · الطبقات · التضارب · الأحداث
  request-windows.js  نوافذ الاستئذان (٣ أيام) وتصحيح البصمة (٧ أيام)
  ── وحدات تجرّ firebase (لا تُختبر في node) ──
  tasks.js · announcements.js · calendar-io.js
js/pages/             صفحة لكل شاشة، تُصدّر render(view, token)
js/components/        attend-panel · employee-form · modals · panels
css/                  ٠٠-tokens → ٠٦-app بالترتيب
firestore.rules       جدار الحماية الحقيقي — ٦٤٠ سطراً موثّقة
bridge/zk_bridge.py   جسر جهاز بصمة ZKTeco → مجموعة zkAttendance
test/                 اختبارات node + محاكي Firestore
```

**الملفات المعلَّمة بـ ⚠️ تحدّد من هو متأخر ومن هو غائب — وعليها يُبنى الخصم من الراتب.
أي تغيير فيها يغيّر رواتب الناس بصمت. عدّل بحذر، وأضف اختباراً لكل حالة.**

---

## نموذج البيانات الحالي

| المجموعة | الملاحظات |
|---|---|
| `users/{uid}` | الموظف. القراءة: أدمن · نفسه · `sameDept()` للمدير. الكتابة الذاتية محصورة في ٦ حقول |
| `settings/{doc}` | `shifts` · `departments` · `branches` · `dateExceptions` · `leaveTypes` · إعدادات المسير. أدمن فقط |
| `requests/{id}` | الاستئذانات والإجازات + سلسلة الاعتماد `chain`/`step` |
| `attendance/{uid}_{date}` | التسجيل من الجوال — `sessions[]` مع الموقع والفرع |
| `zkAttendance/{uid}_{date}` | **مصدر مستقل** من جهاز البصمة. `allow write: if false` — الجسر وحده يكتبه |
| `attendanceAdjustments` | تعديلات إدارية. `update, delete: if false` — لا تراجع |
| `attendancePhotos` · `payrollRuns` · `hrTickets` · `auditLog` · `leaderboard` | — |
| `tasks/{id}` + `messages` | المهام — `departments` مصفوفة، والمحادثة إنشاء-فقط |
| `announcements/{id}` + `acks` | إعلانات الأدمن — الكتابة له وحده فلا ثغرة إغراق |
| `calendarEvents/{id}` | أحداث التقويم — `department` فارغة = الشركة كلها |

**دوال القواعد المتاحة لإعادة الاستعمال:** `isAdmin()` · `isMgr()` · `isActive()` ·
`sameDept()` · `isMine(uid)` · `only([...])` · `fresh(t)` · `todayKsa()` · `isYmd(s)`.
استعملها ولا تكتب مثيلاتها.

---

## مزالق موثّقة — اقرأها قبل أن تكتشفها بالطريقة الصعبة

1. ~~`attend-panel.js:155`~~ — **أُصلح في المرحلة ١.** القرار انقسم إلى قرارين:
   `attendButtonState()` النقيّة في `shifts.js` تفحص الجلسة المفتوحة قبل أي شرط
   زمني، فالانصراف لا يُقفل أبداً. و`checkInAllowed()` تحكم الحضور وحده.
2. **صفحة `team-perf` مفتوحة للمدير، وبقية صفحات الحضور ما زالت محجوبة عنه.**
   الفرق أن استعلامها مقيَّد بـ `where('department','==',…)` ويقابله فهرس
   `(department, date)` في `firestore.indexes.json`. أي صفحة حضور أخرى تُفتح له
   تحتاج الشيئين معاً — الاستعلام المقيَّد **والفهرس منشوراً** — وإلا شاشة فارغة
   بخطأ صلاحيات. ✅ الفهارس والقواعد والاستضافة نُشرت كلها بتاريخ ٢٠٢٦-٠٨-١٢ على
   مشروع `seen-hr2` — أي فهرس جديد بعد هذا التاريخ يحتاج نشراً من جديد.
3. **`sameDept()` تمنح المدير قراءة وثيقة الموظف كاملة — بما فيها `salary`.**
   **مقصود بقرار المالك (٢٠٢٦-٠٨-١٢): «ما فيه مشكلة لو مدير القسم شاف الرواتب».**
   إخفاء العمود في `employees.js` تجميل مقصود لا حماية، ونقل الحقول المالية إلى
   وثيقة فرعية **مُلغى**. المدير يقرأ ولا يكتب: قاعدة الإنشاء تشترط `!('salary' in d())`.
4. **لا تُنشئ مجموعة `notifications`.** اقرأ التعليق أعلى `js/lib/notifications.js`:
   ثغرة إغراق تحتاج Cloud Functions لسدّها. الإشعارات تُشتقّ من الاشتراكات القائمة.
   (الاستثناء الوحيد: `announcements` في المرحلة ١١ — كتابتها للأدمن وحده، فلا ثغرة.)
5. **الرصيد والحالة يُعدَّلان في معاملة واحدة** (`requests.js:140`, `:193`). فصلهما كان
   يترك طلباً معتمَداً برصيد لم يُخصم. لا تلمسها قبل قراءة تعليقها.
6. **الجلسة تبقى مفتوحة حتى يضغط الموظف «انصراف».** إغلاق الصفحة أو انقطاع الإنترنت
   لا يسجّل انصرافاً، وغياب البيانات لا يعني عدم وجود جلسة مفتوحة.
7. **مصدران مستقلّان للحضور** (الجوال + جهاز ZKTeco) لا يُغني أحدهما عن الآخر: المسير
   من الجهاز، والموقع والصورة من الجوال.
8. **السياج الجغرافي يمنع الخطأ العابر لا الغشّ المتعمّد** — التحقق في متصفح الموظف.
   الضابط الحقيقي هو `fresh()` التي تربط الطابع الزمني بوقت السيرفر.
9. **حدود تعيش في الواجهة وحدها ولا يمكن فرضها في قاعدة** — مكتوبة صراحةً في
   الكود، ولا تُقرأ ضماناً من السيرفر:
   • **قفل الحضور حسب نافذة الوردية** — التحقق يحتاج `get()` على settings و users
     في كل بصمة، وهي قراءة مفوترة. الحارس على السيرفر سقفٌ رخيص (اليوم أو أمس +
     `fresh()`)، والتعويض رصدٌ لا منع: وسم `lateCheckIn` على السجل.
   • **سقف ٣ طلبات تصحيح في الدورة** — عدّ طلبات الموظف يحتاج استعلاماً داخل قاعدة،
     وFirestore لا يقدر عليه.
   • **اعتماديات المهام** — التحقق يحتاج `get()` لكل مانع على كل كتابة.
   • **`delegatedUntil`** — لا مؤقّت على السيرفر، فالواجهة تتجاهل المنتهي والمدير يلغيه.
10. **⚠️ `p().get('field', default)` في قاعدة قراءة قد يُفرِّغ الشرط.** أثناء تقييم
   استعلام سرد لا تكون بيانات الوثيقة متاحة، فترجع `get()` قيمتها الافتراضية. كانت
   `p().get('department','') == ''` تجعل سرد `calendarEvents` كاملاً مقبولاً — بينما
   `getDoc` على وثيقة بعينها يُرفض صحيحاً، فمرّ الخلل بين `get` و`list`. استعمل الوصول
   المباشر `p().field`: يُخطئ حين لا تتوفّر البيانات، والقاعدة التي تُخطئ **ترفض**.
   وتحقّق دائماً باختبار سرد غير مقيَّد لا باختبار `getDoc` وحده.
11. **الصفحات التفصيلية تحتاج إدخالاً في `DETAIL_PAGES`** داخل `js/config/pages.js`.
   `canOpen()` تقرأ `NAV_GROUPS` وحدها، وصفحة تُفتح من صفّ جدول ليست فيها — فيُعاد
   المستخدم للرئيسية بلا رسالة. حصل هذا مع صفحة المهمة.

---

## خطة العمل — ١٢ مرحلة

**التفاصيل الكاملة لكل مرحلة (نموذج البيانات · القواعد · الواجهة · الاختبارات) في
[`PROMPT-new-features.md`](./PROMPT-new-features.md).** لا تبدأ مرحلة قبل قراءة قسمها.

| # | المرحلة | تعتمد على | الحجم | الحالة |
|---|---|---|---|---|
| ٢ | **نظام الشفتات المتعدد** — خطط شفتات مُسمّاة تُسند لأقسام أو أفراد | — | كبير | ☑ |
| ١ | **قفل تسجيل الحضور** بعد نهاية الوردية، والانصراف يبقى متاحاً دائماً | ٢ | متوسط | ☑ |
| ٦ | **خريطة لتحديد موقع الفرع** — Leaflet + OpenStreetMap بدل الإحداثيات | — | صغير | ☑ |
| ٣ | **صلاحيات مدير القسم** — يرى ويضيف موظفي قسمه فقط | — | متوسط | ☑ |
| ٤ | **متابعة أداء موظفي القسم** ونسب الانضباط | ٣ | متوسط | ☑ |
| ٥ | **نظام المهام — الأساس**: إنشاء · تكليف · دورة حياة · محادثة · تحليلات | ٣ | كبير جداً | ☑ |
| ١١ | **الرسائل الجماعية** من الموارد البشرية مع إقرار بالاطّلاع | — | متوسط | ☑ |
| ٨ | **رصيد الإجازات** — الأدمن يحدّد المستحقّ ثم النظام يحسبه تلقائياً | — | كبير | ☑ |
| ١٠ | **تصحيح بصمة ناقصة** بطلب ذاتي يعتمده المدير | — | متوسط | ☑ |
| ٧ | **نظام المهام — التوسعات الثمانية** (متكرّرة · قوالب · ربط بالغياب · وقت فعلي · اعتماديات · تفويض · أقسام مشتركة · أرشيف) | ٥ | كبير | ☑ |
| ٩ | **تقويم الفريق** — الإجازات والشفتات والعطل في شاشة واحدة | ٢ · ٨ | متوسط | ☑ |
| ١٢ | اقتراحات مؤجَّلة — **للنقاش لا للتنفيذ** | — | — | لم تُنفَّذ عمداً |

**نُفِّذت بالترتيب:** `٠ ← ٦ ← ٢ ← ١ ← ٣ ← ٤ ← ٥ ← ١١ ← ٨ ← ١٠ ← ٧ ← ٩`

⚠️ المرحلة ٠ لم تكن في الخطة الأصلية وأُضيفت لأنها حاجز: `firestore.indexes.json`
لم يكن موجوداً أصلاً و`firebase.json` لا يشير إليه، فأمر نشر الفهارس كان يفشل —
وأربع مراحل تعتمد عليه.

سبب الترتيب:

- **٢ قبل ١** — قفل الحضور يعتمد على نموذج الشفتات الجديد. القفل الثابت على ٤:٠٠ يشلّ
  موظفاً دوامه يبدأ ٣ العصر.
- **٣ قبل ٤ و٥** — صلاحيات مدير القسم أساس شاشتَي الأداء والمهام.
- **١١ مبكراً** — مستقلّة تماماً، خطرها منخفض وأثرها فوري.
- **٨ قبل ٩** — التقويم يعرض الإجازات.
- ~~**٧ بعد استقرار ٥** أسبوعين~~ — **أُلغيت القاعدة بقرار المالك:** ٧ تُبنى مع ٥ في
  نفس الدفعة.

### قرارات متّخذة (لا تُناقَش مجدداً)

- قفل الحضور **مرتبط بالوردية** لا بساعة ثابتة.
- **زر واحد متحوّل** لا زرّان — تُضاف حالة رابعة فقط.
- الخريطة: **Leaflet + OpenStreetMap** (مجاني، بلا مفتاح API).
- مجموعة `tasks` تُنشأ بـ **`departments: []` مصفوفة من اليوم الأول** حتى لو الواجهة
  تسمح بقسم واحد — يوفّر ترحيلاً مؤلماً لاحقاً.
- الموظف **لا يضع مهمته `done` بنفسه** — يضعها `review` والمدير يعتمدها.

### قرارات معلَّقة تحتاج موافقة المالك قبل التنفيذ

- ~~**المرحلة ١:** الحضور المتأخر~~ — **حُسم: مسموح** لمن لم يسجّل اليوم إطلاقاً، موسوماً
  `lateCheckIn`. `settings.allowLateCheckIn` افتراضها `true`.
- ~~**المرحلة ٣:** نقل الحقول المالية~~ — **مُلغى بقرار المالك.** مدير القسم يرى رواتب
  قسمه، ولا ترحيل بيانات.
- **المرحلة ٨:** لا تُطبَّق أي سياسة إجازات أو ترحيل رصيد قبل عرض جدول معاينة يقارن رصيد
  كل موظف قبل وبعد.

---

## طريقة العمل على أي مرحلة

1. اقرأ قسم المرحلة في `PROMPT-new-features.md` **كاملاً**، ثم اقرأ الملفات التي يذكرها.
2. اعرض خطة: الملفات التي ستلمسها · شكل البيانات الجديد · القواعد الأمنية · ما قد ينكسر.
   **انتظر الموافقة.**
3. نفّذ: `firestore.rules` أولاً، ثم `js/lib/` (المنطق النقي)، ثم الواجهة. الترتيب
   يمنع بناء شاشة على قاعدة تُصمَّم لاحقاً فترفضها.
4. اكتب الاختبارات مع الكود لا بعده — بالإثبات والنفي معاً لكل قاعدة جديدة.
5. أنشئ الفهارس المركّبة في `firestore.indexes.json` **وانشرها** قبل فتح الشاشة.
6. اجتز قائمة التحقق في آخر `PROMPT-new-features.md`.
7. حدّث الحالة (☐ ← ☑) في جدول المراحل أعلاه.

### قائمة التحقّق قبل إعلان أي مرحلة منتهية

- [ ] `npm test` كامل بلا اختبار مكسور (١١٠٢ فأكثر)
- [ ] اختبارات جديدة للميزة وحالاتها الحدّية
- [ ] `test/rules.test.mjs` لكل قاعدة جديدة — بالإثبات والنفي
- [ ] كل استعلام جديد يطابق قاعدته حرفياً
- [ ] الفهارس المركّبة منشورة
- [ ] لا حقل إلزامي جديد — كل غياب يطابق سلوك اليوم
- [ ] سقف على كل حقل يكتبه المستخدم
- [ ] `npx eslint js/` نظيف
- [ ] جُرِّب بثلاثة حسابات: أدمن · مدير قسم · موظف
- [ ] جُرِّب على شاشة جوال — النظام يُستعمل من الجوال أكثر من سطح المكتب
- [ ] تعليق عربي يشرح «لماذا» فوق كل قرار غير بديهي
- [ ] لا وعد أمني في الواجهة بلا قاعدة تحرسه على السيرفر

---

## أسلوب الكود

- عربي RTL في كل النصوص الظاهرة. التعليقات بالعربية.
- ES modules، `import`/`export` صريح. لا مكتبات جديدة بلا سبب مقنع — كل تبعية تُحمَّل من
  CDN وتدخل في `Content-Security-Policy` في `firebase.json`.
- أعد استعمال `js/lib/ui.js` (`card` · `empty` · `tableWrap` · `button` · `stat` ·
  `grid` · `callout`) و`js/lib/dom.js` (`el` · `esc` · `openModal` · `toast`).
  **لا تكتب HTML خاماً لمكوّن موجود.**
- `esc()` على كل نص من المستخدم يدخل `innerHTML` — بلا استثناء.
- كل صفحة تُصدّر `render(view, token)` وتتحقق من `isStale(token)` بعد كل `await`.
- المؤقّتات والاشتراكات عبر `js/lib/lifecycle.js` (`setPageInterval` ·
  `trackSubscription` · `onCleanup`) — وإلا تسرّبت بين الصفحات.
- **حسبة واحدة في مكان واحد.** إن احتجت منطقاً موجوداً بنطاق مختلف، عمّمه في `lib/`
  واستعمله من الاثنين. حسبتان للتأخير تتباعدان تعني رقمين مختلفين لنفس الموظف في شاشتين.
